---
title: API
description: The REST API reference for every Hanzo Cloud service -- one base URL, one bearer key, at api.hanzo.ai/v1.
---

<DocsHero />

## Get going [#get-going]

Install the CLI and sign in. Two commands, then you have a key.

```bash
curl -fsSL hanzo.sh | bash
hanzo auth login
```

`hanzo auth login` opens hanzo.id in your browser and writes the credential locally,
so every later command — and every SDK on this machine — is authenticated. To call
the API directly instead, mint a key with [API Keys](/docs/api-keys) and send it as a
bearer token.


Every Hanzo service exposes a REST API under one base URL. One credential works across all of them, and the request shape is uniform: a bearer token, a JSON body, a JSON response.

## Base URL and Authentication

```
https://api.hanzo.ai/v1
```

Authenticate every request with a bearer token in the `Authorization` header. Use an API key (`hk-...`) for server-to-server calls, or an IAM access token for user-facing apps:

```
Authorization: Bearer hk-...
```

All traffic is TLS-only and passes through the [Gateway](/docs/gateway), which authenticates, rate-limits, and routes it. See [API Keys](/docs/api-keys) to create a key and [Authentication](/docs/authentication) for OIDC tokens.

## Service Namespaces

Each service is namespaced under `/v1`:

| Path | Service |
|------|---------|
| `/v1/chat/completions`, `/v1/embeddings`, `/v1/models` | AI API (OpenAI-compatible) |
| `/v1/iam` | [Identity, orgs, and roles](/docs/iam) |
| `/v1/kms` | [Secrets and keys](/docs/kms) |
| `/v1/authz` | [Authorization checks](/docs/authz) |
| `/v1/mpc` | [Threshold signing](/docs/mpc) |
| `/v1/billing`, `/v1/pricing` | [Usage, credits, and pricing](/docs/billing) |

The API is versioned at `v1`; it evolves additively, so integrations stay stable.

## Example Request

```bash
curl https://api.hanzo.ai/v1/models \
  -H "Authorization: Bearer hk-..."
```

Responses are JSON. Errors use standard HTTP status codes -- `401` for a bad or missing key, `403` when authenticated but forbidden, `429` when rate-limited, `402` when out of credits. [Auth](/docs/architecture/auth) explains what each one means here, and how to reproduce them.

## Why every surface agrees

This REST API, the [SDKs](/docs/sdks), the [CLI](/docs/cli) and the [MCP tools](/docs/mcp) are not four integrations kept in step -- they are four projections of one declaration. See [The One Registry](/docs/architecture/registry).

## Related

- [API Keys](/docs/api-keys) -- create and manage `hk-` credentials
- [Authentication](/docs/authentication) -- OAuth 2.0 + OIDC for user tokens
- [Auth](/docs/architecture/auth) -- what a token proves, how org and project scope it, 401 vs 403
- [The One Registry](/docs/architecture/registry) -- the one declaration every surface is projected from
- [Transport](/docs/architecture/transport) -- ZAP and HTTP, and which wire a call takes
- [Gateway](/docs/gateway) -- the entry point every request passes through
- [OpenAPI](/docs/openapi) -- machine-readable specs and generated references
- [SDKs](/docs/sdks) -- Python, TypeScript, Go, and Rust clients
