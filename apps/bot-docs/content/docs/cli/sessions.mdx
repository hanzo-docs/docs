---
title: "`bot sessions`"
description: "CLI reference for `bot sessions` (list stored sessions + usage)"
---

List stored conversation sessions.

```bash
bot sessions
bot sessions --active 120
bot sessions --json
```
